generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./deterministic.sqlite"
}

model DeterministicSnapshotRecord {
  id           Int      @id @default(autoincrement())
  provider     String
  snapshotJson String
  receivedAt   DateTime @default(now())

  @@index([provider, receivedAt])
}
